# ==============================
# Utilities
# ==============================


# ==============================
# Application / UI
# ==============================

streamlit
# Builds the AttendX web interface and connects the UI with the AI and attendance functionality

numpy
# Numerical operations and handling arrays/matrices used in image and ML processing

pandas
# Managing attendance records and other structured/tabular data


# ==============================
# Machine Learning / Face Recognition
# ==============================

scikit-learn
# Machine learning utilities such as classification, preprocessing, and model prediction

dlib-bin
# Provides dlib's precompiled binaries for facial detection and face recognition processing

# For Python 3.13 on Windows, install the downloaded dlib wheel manually when required
# python -m pip install dlib-20.0.99-cp313-cp313-win_amd64.whl

git+https://github.com/ageitgey/face_recognition_models
# Provides pretrained face-recognition model files used by the dlib face recognition system

# setuptools<70.0.0
# Keeps setuptools below version 70 for compatibility with older face-recognition/dlib dependencies


# ==============================
# Database & Authentication
# ==============================

supabase
# Connects AttendX to Supabase for storing and retrieving student, teacher, and attendance data

bcrypt
# Securely hashes and verifies passwords so they are not stored as plain text


# ==============================
# QR / Image Processing
# ==============================

segno
# Generates QR codes for attendance or user verification

pillow
# Opens, processes, and manipulates images used by the face recognition system


# ==============================
# Voice Recognition / Audio Processing
# ==============================

librosa
# Loads and processes audio signals and extracts audio features for voice recognition

soundfile
# Reads and writes audio files used by the voice recognition pipeline

torch
# Deep learning framework required by Resemblyzer for speaker embeddings

torchaudio
# PyTorch audio processing utilities

webrtcvad-wheels
# Provides prebuilt WebRTC Voice Activity Detection binaries without requiring C++ compilation

typing
# Provides the typing package required by the Resemblyzer package

resemblyzer
# Generates speaker embeddings used for voice-based student identification